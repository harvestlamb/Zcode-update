"""ZCode mirror collector — runs on the internet-facing machine.

Run ``python -m collector.export`` to build a complete ``.zdoc`` package, or
run individual scripts for partial steps (crawler, assets, markdown, etc.).
"""
